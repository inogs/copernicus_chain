#!/bin/ksh
# This command has to be executed after a checkout of the OPA chain

# Load common profile
. @@(I:OPA_HOME)/bin/opa_profile.inc

for file in "$@" ; do
  opa_prex "$OPA_BINDIR/split -d -b 20971520 -a 3 $file ${file}_part"
done

